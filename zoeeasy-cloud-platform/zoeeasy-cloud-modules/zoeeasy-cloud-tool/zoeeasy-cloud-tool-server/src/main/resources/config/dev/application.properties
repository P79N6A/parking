app.id=20180901100010
# will inject 'application' namespace in bootstrap phase
apollo.bootstrap.enabled=true
# will inject 'application' and 'FX.apollo' namespaces in bootstrap phase
apollo.bootstrap.namespaces=application,common,dubbo,database,oss
# private namespace will be inject in bootstrap phase
apollo.namespace.private.need.default=true
# public namespace will be inject in bootstrap phase
# apollo.namespace.commons=

spring.profiles.active = @activatedProfile@
logging.level.root = INFO
logging.level.com = INFO
logging.level.dubbo = INFO