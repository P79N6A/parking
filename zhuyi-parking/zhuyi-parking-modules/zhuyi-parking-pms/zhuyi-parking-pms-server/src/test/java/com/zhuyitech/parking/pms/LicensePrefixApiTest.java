/*
package com.zhuyitech.parking.pms;

import com.scapegoat.infrastructure.core.dto.result.ResultDto;
import com.scapegoat.infrastructure.dubbo.starter.QuickDubboBootstrap;
import com.scapegoat.infrastructure.dubbo.test.anotation.ScapegoatDubboApplicationConfiguration;
import com.zhuyitech.parking.pms.dto.request.license.LicensePrefixAddRequestDto;
import com.zhuyitech.parking.pms.service.api.LicensePrefixService;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import static org.junit.Assert.assertTrue;

@RunWith(SpringJUnit4ClassRunner.class)
@ScapegoatDubboApplicationConfiguration(QuickDubboBootstrap.class)
public class LicensePrefixApiTest {

    @Autowired
    private LicensePrefixService licensePrefixAppService;

    */
/**
     * 根据品牌获取车型
     *//*

    @Test
    public void LicensePrefixAddTest() {
        LicensePrefixAddRequestDto licensePrefixAddRequestDto = new LicensePrefixAddRequestDto();
        licensePrefixAddRequestDto.setName("浙");
        licensePrefixAddRequestDto.setParentId(null);
        licensePrefixAddRequestDto.setType(1);
        ResultDto resultDto = licensePrefixAppService.addLicensePrefix(licensePrefixAddRequestDto);
        assertTrue(resultDto.isSuccess());
    }
}
*/
